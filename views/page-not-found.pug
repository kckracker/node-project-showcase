extends layout 

block content 
    //- Adding friendly not found message and pulling in photo I created to serve as background. Adding text area for dev stack to create resizable space for these details.
    div.not-found-background    
        h1.not-found No pages found, just a campsite...
        h2.not-found= error.message
        div
        h2.not-found= `Error ${error.status}`
            h3.not-found More dev details: 
            textarea.not-found(rows= 20)= error.stack
                